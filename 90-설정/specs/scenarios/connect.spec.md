# 연결 시나리오 Spec (MOC)

## 트리거 키워드
- "MOC", "맵", "주제 연결", "관계 구조"

## 워크플로우 요약
1. 템플릿 로드: `Filesystem:read_file("/90-설정/템플릿-MOC.md")`.
2. 프론트매터 채우기: `tags`, `topics`, `moc_links`(상위/관련 MOC), `projects`를 적절히 입력한다 (`core/metadata.spec.md`).
3. 본문 작성: 템플릿 섹션(개요 → 주제 지도 → 학습 경로 → 문헌 → 관련 주제 → 프로젝트 → Dataview → 시각화 → 인사이트)을 그대로 따른다.
4. 핵심개념 최소 3개 이상을 선택하고, 각 링크 옆에 관계를 한 줄 설명한다.
5. 저장: `/30-연결/맵-주제.md`.

## 메타데이터 집중 포인트
- `topics`: MOC가 대표하는 핵심 주제를 2~4개 기입.
- `moc_links`: 상위/관련 MOC를 구분해 배열에 기록 (자기 참조 허용).
- `summary`: Dataview에서 미리보기할 수 있도록 140자 내외로 작성.
- `projects`: 이 MOC가 지원하는 주요 프로젝트 ID를 연결.

## 콘텐츠 규칙
- `## 🗺️ 주제 지도` 섹션에서 핵심개념 3개 이상을 wikilink와 설명으로 기록한다.
- `## 📖 주요 문헌`과 `## 🎯 프로젝트`는 각각 최소 2개 항목을 유지한다.
- Dataview/mermaid 블록은 템플릿 그대로 두고 필요한 값만 채운다 (쿼리 대상 태그 갱신 등).
- 새 핵심개념 생성 시, 해당 개념에 `[[맵-주제]]` 역링크를 추가한다.

## 검증 체크리스트
- [ ] 핵심개념 링크가 3개 이상이다.
- [ ] `moc_links` 배열이 채워져 있다.
- [ ] Dataview 쿼리에 사용된 태그가 존재한다.
- [ ] Mermaid 다이어그램 블록이 유지된다.
- [ ] 요약(`summary`)이 비어있지 않다.

> Validator: `validators/link-validator.spec.md` + `core/metadata.spec.md`.
