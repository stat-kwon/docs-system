# 생성 시나리오 Spec (핵심개념)

## 트리거 키워드
- "핵심개념", "permanent note", "개념 정리"
- "노트 만들어줘", "이 개념 설명해줘"

## 워크플로우 요약
1. 템플릿 로드: `Filesystem:read_file("/90-설정/템플릿-Permanent.md")`.
2. 프론트매터 유지: 템플릿 필드를 그대로 두고 `source`, `moc_links`, `topics`, `related_notes`를 채운다 (`core/metadata.spec.md` 참고).
3. 본문 작성: 템플릿 섹션(핵심 정의 → 상세 설명 → 왜 중요한가 → 통찰 → 연결 → 적용 → 질문)을 순서대로 채워 100줄 이내를 유지한다.
4. 링크 강화: `## 🔗 연결` 아래에 MOC/관련 개념/출처를 명확한 설명과 함께 작성한다.
5. 저장: `/20-정리/핵심개념/개념-YYYYMMDD{suffix}-제목.md`.

## 메타데이터 집중 포인트
- `source`: 바로 직전에 만든 자료정리 또는 원문을 wikilink로 연결한다.
- `moc_links`: 최소 1개의 MOC를 연결하고, 역링크도 생성한다.
- `topics`: 핵심 키워드를 1~3개 기입하여 검색성을 높인다.
- `projects`: 해당 개념이 참여하는 프로젝트가 있다면 프로젝트 ID를 추가한다.
- `summary`: Dataview에서 미리보기할 수 있도록 한 문장으로 정리한다.

## 품질 규칙
- 한 노트는 하나의 개념만 다룬다.
- 복사/붙여넣기 금지: Literature Note와 동일 문장을 반복하지 않는다.
- 연결 섹션에서 각 링크에 간단한 관계 설명을 포함한다.

## 검증 체크리스트
- [ ] `source` 필드 존재 및 파일 확인.
- [ ] `moc_links`에 최소 1개 이상의 wikilink.
- [ ] `topics` / `tags`가 채워져 있음.
- [ ] 본문 길이 100줄 미만.
- [ ] 템플릿 섹션 순서 유지.

> Validator: `validators/link-validator.spec.md` + `core/metadata.spec.md`.
