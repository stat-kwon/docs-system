# 캡처 시나리오 Spec (즉흥메모)

## 트리거 키워드
- "저장해줘", "메모", "아이디어", "TODO", "질문"
- 명확한 시나리오가 아닐 때 기본값으로 고려

## 워크플로우 요약
1. 템플릿 로드: `Filesystem:read_file("/90-설정/템플릿-즉흥메모.md")`.
2. 프론트매터 채우기: `type`(idea/question/todo/reference), `status`, `tags`, `summary`, `projects`를 적절히 설정한다 (`core/metadata.spec.md` 참고).
3. 본문 작성: 템플릿의 `## 내용` 및 `## 다음 단계` 섹션만 채우고 구조는 유지한다.
4. 저장: `/10-수집/즉흥메모/YYYYMMDD-HHMM-제목.md`.

## 메타데이터 집중 포인트
- `summary`: 한 줄로 아이디어/할 일을 요약해 검색 결과 품질을 높인다.
- `projects`: 관련 프로젝트가 있다면 프로젝트 ID를 추가하여 후속 관리가 가능하도록 한다.
- `tags`: 최소 1개의 도메인 태그를 부여한다 (`#idea/product`).
- TODO 타입일 경우 `status`를 `pending`으로 유지하고 체크박스를 활용한다.

## 품질 규칙
- TODO 형식: `- [ ]` Tasks를 사용하고 긴급도 이모지를 텍스트로 남긴다 (예: `- [ ] DB 마이그레이션 🔴`).
- 링크는 선택 사항이지만, 완료 시 관련 노트에 링크를 추가하면 validator 경고를 줄일 수 있다.
- 첨부파일이 있다면 `/80-보관/첨부파일/YYYYMMDD/` 폴더에 복사하고 `attachments_from_source` 필드를 추가한다.

## 검증 체크리스트
- [ ] 저장 경로와 파일명이 규칙을 따른다.
- [ ] 프론트매터 필수 필드(`type`, `status`, `created`, `tags`)가 채워져 있다.
- [ ] TODO일 경우 Tasks 형식과 우선순위 이모지 사용.
- [ ] `summary`가 비어있지 않다.
- [ ] 첨부파일이 있다면 메타데이터가 동기화되어 있다.

> Validator: `validators/priority.spec.md` + `core/metadata.spec.md`.
